// Se quiere un bucle que genere una lista con todos los números pares positivos por debaje del número tecleado por el usuario.

let usuario = prompt("Di un número");
for (let i = 2; i <= usuario; i++) {
    if (i % == 0) {
        console.log(`El número ${i} es par`);
    }
}