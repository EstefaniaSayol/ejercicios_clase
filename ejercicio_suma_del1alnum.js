/*Escriba un programa que encuentre la suma de todos los números del 1 al num. 
El número siempre será un entero positivo mayor que cero*/

let summation = function (num) {
    let result = 0;
    lei i = 1;
    while (i <= num) {
        result +=
    }
}

return result;
summation(10);
